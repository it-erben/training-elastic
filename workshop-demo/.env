# Kurs-Umgebung. Standard: Tag-3-Cluster (environment/day3, Security an, TLS).
# workshop-demo wird nur in Lab 08 (Tag 3) verwendet.
ELASTIC_URL=https://localhost:9200
ELASTIC_USER=elastic
ELASTIC_PASSWORD=changeme
ELASTIC_TLS_VERIFY=false

# Tag 1/2 (Security aus): die vier Zeilen oben auskommentieren und nur diese
# eine aktivieren:
# ELASTIC_URL=http://localhost:9200
